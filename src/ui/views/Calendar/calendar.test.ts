import { describe, expect, it } from "@jest/globals";
import { startOfWeek, endOfWeek, getFirstDayOfWeek } from "./calendar";

describe("generateDateRange", () => {
  it("month range", () => {});
});
